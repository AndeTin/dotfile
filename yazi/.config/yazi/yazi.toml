[opener]
gnome_gio = [
    { run = 'gio open "$1"', desc = "Open", block = false }
]

[open]
rules = [
    # Match PDFs specifically
    { url = "*.pdf", use = "gnome_gio" },
    { mime = "application/pdf", use = "gnome_gio" },

    # Match Office files
    { url = "*.doc*", use = "gnome_gio" },
    { url = "*.xls*", use = "gnome_gio" },

    # Fallback: If it's not a text file or folder, try gio
    { mime = "*", use = "gnome_gio" }
]

# Keep your existing previewers below
[[plugin.prepend_previewers]]
mime = "application/{*zip,tar,bzip2,7z*,rar,xz,zstd,java-archive}"
run  = "ouch"
